# ==============================================================================
# homepage — service-specific environment (ADR-0012)
# Copy to `.env.service` on the host, chmod 600.
# Platform globals come from the root .env via the `.env` symlink.
# ==============================================================================

# No service-specific variables today. HOMEPAGE_ALLOWED_HOSTS is derived from
# the global DOMAIN in compose.yaml. Widget API tokens would go here if added.
