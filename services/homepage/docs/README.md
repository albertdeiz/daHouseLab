# homepage — Documentation

Deep documentation for the dashboard. Front page: [`../README.md`](../README.md).

The dashboard's content (tiles, widgets, bookmarks) is runtime configuration in
`${CONFIG_ROOT}/homepage/*.yaml` — document notable widget integrations and their API-token
handling here as they are added ([documentation conventions](../../../docs/standards/documentation-conventions.md)).
