# ==============================================================================
# uptime-kuma — environment template
# Copy to `.env` on the host and fill in. NEVER commit `.env`.
# Conventions: docs/standards/environment-variables.md
# ==============================================================================

# --- Platform (values come from the root .env)
TZ=America/Santiago
DATA_ROOT=/srv/dahouselab/data

# --- Service
# No service-specific variables: monitors, users and notifications are
# configured in the web UI and stored in the SQLite database.
