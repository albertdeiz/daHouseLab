# ==============================================================================
# uptime-kuma — service-specific environment (ADR-0012)
# Copy to `.env.service` on the host, chmod 600.
# Platform globals come from the root .env via the `.env` symlink.
# ==============================================================================

# No service-specific variables: monitors, users and notifications are
# configured in the web UI and stored in the SQLite database.
