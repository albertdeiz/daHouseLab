# ==============================================================================
# vaultwarden — service-specific environment (ADR-0012)
# Copy to `.env.service` on the host, chmod 600. NEVER commit `.env.service`.
# Platform globals come from the root .env via the `.env` symlink.
# ==============================================================================

# Open ONLY while creating your own account (deploy runbook), then false forever.
SIGNUPS_ALLOWED=false

# Admin panel (/admin) token — LEAVE UNSET to keep the panel disabled (current deployment).
# To enable: argon2 hash via `docker run --rm -it vaultwarden/server:1.34.3 /vaultwarden hash`,
# store the plaintext inside the vault itself, paste the hash here in single quotes.
# ADMIN_TOKEN=''
